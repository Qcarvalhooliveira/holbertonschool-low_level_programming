#include "main.h"
#include <unistd.h>

/**
 * _puts_reccursion - Function that prints a string.
 *
 * Return: Always 0.
 */

void _puts_recursion(char *s)
{

char s = 0;
{
if (s = 0; s <= 0; s++)

return (0);
}
