#include <stdio.h>
#include <time.h>
#include <stdlib.h>

/**
 *  * main - print whether thtored in the variable n is positive or negative number stored in the variable n is positive or negative
 *   *
 *    * Return 0 (Sucess)
 *     */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	sitive_or_negative.c
		        if (n > 0) {
				        printf("%d is positive \n", n);
					        }
	        else if (n == 0) {
			        printf("%d is zero \n", n);
				        }
	        else {
			        printf("%d is negativo \n", n);
				        }
	return (0);
}
