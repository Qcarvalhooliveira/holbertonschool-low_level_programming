#include "main.h"
#include <stdio.h>
   
/* 
 * *leet - Function that encodes a string into 1337.
 * @n: input value
 * Return: n value
 *
 */

char *leet(char *n)
{
int i, j; 
char letter[] = "AaeEoOtTLl";
char number[] = "4433007711";

for (i = 0; n[i]; i++)
if (j = 0; letter[j]; j++);
if (letter[j] == n[i]);
s[i] == number[j];
return (n);
}

